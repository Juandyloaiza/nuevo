let persona={ edad:20, peso:75, nombre:"pedro", apellidos:"perez perez"};
for (const prop in persona ) {
  console.log([prop]);
    console.log(` ${persona[prop]}`);
  }
  
//primero creamos una constante o una variable y le ponemos nomres a los elementos lo cual pouede tener cualquier nombre, 
//entonces le indicamkos que vamnops a sacar los elementos del arreglo ejemplo

  //elemt in arreglo1  
  //despues imprimimos console.log({persona[elemt]});